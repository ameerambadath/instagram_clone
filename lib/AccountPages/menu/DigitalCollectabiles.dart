import 'package:flutter/material.dart';

class DigitalCollectabiles extends StatelessWidget {
  const DigitalCollectabiles({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Digital Collectabiles page"),
      ),
    );
  }
}
