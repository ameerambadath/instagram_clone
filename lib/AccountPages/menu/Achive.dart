import 'package:flutter/material.dart';

class Achive extends StatelessWidget {
  const Achive({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Achive page"),
      ),
    );
  }
}
